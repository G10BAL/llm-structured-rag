[project]
name = "llm-structured-rag"
version = "0.1.0"
description = "Project to make OPENAI answer using certain structure by validating with pydantic"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "chromadb>=1.5.9",
    "openai>=3.3.1",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.3",
]
